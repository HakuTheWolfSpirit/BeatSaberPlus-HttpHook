using ChatPlexMod_ChatIntegrations.Interfaces;

namespace BeatSaberPlus_HTTPHook.ChatIntegrations
{
    internal static class HTTPHookTriggerType
    {
        internal static readonly ETriggerType TriggerType = (ETriggerType)(ETriggerType._CUSTOM_START + 10);
    }
}
